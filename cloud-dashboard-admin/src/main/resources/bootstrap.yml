server:
  port: 8040

spring:
  application:
    name: cloud-admin-dashboard

eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
            defaultZone: http://localhost:8761/eureka/